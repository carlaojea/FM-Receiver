from machine import Pin, I2C
import time

SDA_PIN = 21
SCL_PIN = 22

i2c = I2C(1, scl=Pin(SCL_PIN), sda=Pin(SDA_PIN), freq=100000)

print("=== I2C SCAN TEST ===")

while True:
    devs = i2c.scan()
    print("Devices:", devs)
    time.sleep(2)

